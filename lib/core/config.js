// Globale App-Konfiguration für das WebApp-Core Framework
window.appConfig = {

    // Titel der App (wird im Header angezeigt)
    appTitle: "Meine WebApp",

    // Optionaler Untertitel der App
    appSubtitle: "Eine Demo der Header-Komponente",

    // (optional) weitere globale Einstellungen für die Zukunft
    theme: "light", // oder "dark"

    // (optional) Routing-Konfiguration
    defaultPage: "index.html"
};
